name: CI of Linux
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: docker build -t cppcheck-alpine cppcheck
      - name: test
        run: docker run --rm -it cppcheck-alpine cppcheck --version
